import React from "react";

export const GifGrid = ({ category }) => {
  console.log(category);

  return (
    <>
      <h3>{category}</h3>
      <p>hola</p>
    </>
  );
};

export default GifGrid;
